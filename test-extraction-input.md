# 后端架构升级方案评审

## 1. 背景

随着用户量增长到 50 万，当前的单体架构已经无法满足性能需求。上周的线上事故（#INC-20260428）暴露了数据库连接池耗尽的问题。本方案旨在对后端进行微服务化改造。

## 2. 技术选型决策

### 2.1 数据库选型

经过团队讨论，**决定使用 PostgreSQL 替换现有的 MySQL**。理由如下：
- PostgreSQL 的 JSONB 类型更适合我们的半结构化数据
- 更好的并发性能，支持 MVCC
- 团队中有 3 人有 PostgreSQL 生产经验

> 反对意见（张三）：迁移成本太高，建议继续优化 MySQL 的索引策略。但经评估，MySQL 的优化空间已经有限，迁移的长期收益大于短期成本。

### 2.2 消息队列选型

**确认采用 Kafka 作为消息队列**，不再考虑 RabbitMQ。原因是：
- 预计日消息量将超过 1 亿条
- Kafka 的吞吐量远高于 RabbitMQ
- 需要消息回溯能力

### 2.3 缓存方案

暂不引入 Redis Cluster，**先使用单机 Redis + 本地缓存的二级缓存方案**。等 QPS 超过 5000 后再考虑集群化。

## 3. 项目计划

### 3.1 里程碑

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| Phase 1: 数据库迁移 | 5月15日-5月30日 | PostgreSQL 主库就绪 |
| Phase 2: 服务拆分 | 6月1日-6月20日 | 用户服务、订单服务独立部署 |
| Phase 3: 消息队列 | 6月21日-7月10日 | Kafka 集群上线 |

### 3.2 人员分工

- 李四：负责数据库迁移和数据同步工具开发
- 王五：负责用户服务的微服务化
- 赵六：负责消息队列的接入和监控

## 4. 风险评估

**已确定以下风险应对策略：**
- 数据迁移期间采用双写方案，确保零数据丢失
- 灰度发布：先迁移 10% 流量，观察一周后再全量切换
- 回滚方案：保留 MySQL 实例 7 天，随时可切回

## 5. 待讨论事项

以下事项尚未达成共识，需要下次会议继续讨论：
- 是否需要引入服务网格（Service Mesh）？
- API 网关选型：Kong vs APISIX？
- 日志收集方案：ELK vs Loki？

## 6. 会议结论

本次评审会议达成以下共识：
1. 微服务化方向确认，不再讨论是否要做
2. 数据库迁移为最高优先级
3. 下周三前完成详细设计文档
