## 当前阶段记忆系统存在问题

1. 决策状态转换：确认后需要转换状态
2. openclaw更新决策信息（例如用户将决策“数据库选型采用PostgreSQL 15”改为决策“数据库选型采用MySQL”）后 git与bitable未同步
3. 在日志里统计调用llm请求次数 初始值为0 一直增加（观察lark-doc是否频繁调用llm）
4. git与bitable要实时同步 决策先写入git里 再同步到bitable里
5. 在用户修正决策冲突（告知openclaw保留哪一个决策）后 需要更新决策冲突状态
6. 决策卡片功能尚未完善 查看详情 更新状态 关联决策均为实现 按钮按下去没反应
7. 给git存储库添加修正决策功能
8. 在bitable里 若决策被修正 需要将原来的决策隐藏 与git同步后更新决策表 需要根据git里的决策状态进行操作
9. 决策被修正过后需要推送决策卡片
10. 在git存储库决策字段里添加前一个关联决策的决策id bitable通过关联决策id，version，决策状态字段判断决策要不要展示 逻辑：同一个决策 若没有冲突 就根据version字段大小展示最新的一个决策 若有冲突 则展示均有冲突字段且没有version比当前决策还大的决策

## 代码修正测试建议
在test目录下新增文件夹进行测试 不需要启动完整服务

测试问题5: 给mcp-server发送请求 然后同时检查mcp-server的回复以及git存储库状态 在调用git存储库修正决策函数/更新决策函数时添加日志输出

测试问题4: 主动修改git存储库的决策 更新该决策的version 一旦version发生变化 就调用同步函数 在git-bitable同步函数里加入日志输出 观察有没有日志出现 git与bitable的同步可以依靠git存储库里决策的version变化进行 